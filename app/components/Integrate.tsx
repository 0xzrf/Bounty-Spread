import { FormDemo } from "../components/Form";

export default function Integrate() {
    return (
        <div className="min-h-screen">
            <FormDemo header="Integrate" subheader="Contact our team to integrate Blinks through API calls to your platform with BountySpread" secondText="Message to send with integration request" className="max-w-[70vw] "/>
        </div>
    )
}